# Code Agent

A Python-based autonomous software engineering agent platform.

## Overview
`code-agent` uses the Anthropic Claude API to perform autonomous coding tasks, providing both a CLI and a Web interface.

## Installation
```bash
make install
```

## Usage
```bash
code-agent --help
```
